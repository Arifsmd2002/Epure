spring.application.name=ProjectEPURE
server.port=8080
spring.main.web-application-type=servlet
spring.devtools.restart.enabled=false

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/epure_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=SMDarif6969
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Thymeleaf
spring.thymeleaf.cache=false

# Razorpay Configuration (Use your real keys in production)
razorpay.key.id=rzp_test_YourKeyIdHere
razorpay.key.secret=YourKeySecretHere

# Static content (removing custom mappings to use defaults)
# Default: /static/** is served at /
# spring.mvc.static-path-pattern=/static/**
# spring.web.resources.static-locations=classpath:/static/

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=arifsmd7989@gmail.com
spring.mail.password=pagzwcvnkwiyvuej
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.debug=true
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2

# Admin email (used as sender / from address)
admin.email=arifsmd7989@gmail.com

# Gemini API Key (for AI Room Analyzer) — Replace with your actual key
gemini.api.key=YOUR_GEMINI_API_KEY_HERE

# File upload size limits (for room image analysis)
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=16MB
